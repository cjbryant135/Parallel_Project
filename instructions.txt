type: git clone <web_address>.git

to commit: git commit -am <comment>
  This will do everything except for new files

add newfiles: git add .

  The . will add all files in the directory, or type list of newfiles
  Then commit these

to pull: git pull origin master

to push: git push origin master

(Note: pull before you push)

  
